---
import Layout from '@layouts/Layout.astro'
import { getCollection } from 'astro:content'

// Get all wiki content
const wikiEntries = await getCollection('wiki')

// Group by country -> city -> category
const groupedWiki = wikiEntries.reduce((acc, entry) => {
  const { country, city, category } = entry.data
  
  if (!acc[country]) acc[country] = {}
  if (!acc[country][city]) acc[country][city] = {}
  if (!acc[country][city][category]) acc[country][city][category] = []
  
  acc[country][city][category].push(entry)
  return acc
}, {} as Record<string, Record<string, Record<string, any[]>>>)

// Helper function to get country flag
const getCountryFlag = (country: string) => {
  const flags: Record<string, string> = {
    'vietnam': '🇻🇳',
    'thailand': '🇹🇭',
    'singapore': '🇸🇬',
    'malaysia': '🇲🇾',
    'indonesia': '🇮🇩',
    'philippines': '🇵🇭',
    'japan': '🇯🇵',
    'south-korea': '🇰🇷',
  }
  return flags[country] || '🌍'
}

// Helper function to get category icon
const getCategoryIcon = (category: string) => {
  const icons: Record<string, string> = {
    'cafe': '☕',
    'coworking': '💼',
    'food': '🍜',
    'vegan-food': '🥗',
    'accommodation': '🏠',
    'transport': '🚗',
    'nightlife': '🌙',
    'activities': '🎯',
    'shopping': '🛍️',
    'health': '🏥',
  }
  return icons[category] || '📋'
}
---

<Layout title="Digital Nomad's Wiki">
  <main class="max-w-6xl mx-auto px-4 py-8">
    <!-- Header -->
    <section class="text-center mb-12">
      <h1 class="text-5xl font-bold mb-4 bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
        🌍 Digital Nomad's Wiki
      </h1>
      <p class="text-xl text-gray-600 dark:text-gray-400 mb-6 max-w-3xl mx-auto">
        Your comprehensive, free guide to the best spots across Southeast Asia and beyond. 
        Real experiences from a fellow nomad, organized by location and category.
      </p>
      <div class="flex justify-center gap-4 mb-8">
        <div class="bg-green-100 dark:bg-green-900/20 text-green-800 dark:text-green-200 px-4 py-2 rounded-lg">
          🆓 Completely Free
        </div>
        <div class="bg-blue-100 dark:bg-blue-900/20 text-blue-800 dark:text-blue-200 px-4 py-2 rounded-lg">
          📍 Detailed Locations
        </div>
        <div class="bg-purple-100 dark:bg-purple-900/20 text-purple-800 dark:text-purple-200 px-4 py-2 rounded-lg">
          🚀 Regularly Updated
        </div>
      </div>
    </section>

    <!-- Wiki Content -->
    <section class="mb-16">
      <div class="grid gap-8">
        {Object.entries(groupedWiki).map(([country, cities]) => (
          <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg overflow-hidden">
            <!-- Country Header -->
            <div class="bg-gradient-to-r from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20 px-6 py-4 border-b">
              <h2 class="text-2xl font-bold flex items-center text-blue-800 dark:text-blue-200">
                <span class="mr-3 text-3xl">{getCountryFlag(country)}</span>
                {country.charAt(0).toUpperCase() + country.slice(1).replace('-', ' ')}
              </h2>
            </div>
            
            <!-- Cities Grid -->
            <div class="p-6">
              <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                {Object.entries(cities).map(([city, categories]) => (
                  <div class="bg-gray-50 dark:bg-gray-700 rounded-lg p-4 hover:shadow-md transition-all duration-300">
                    <h3 class="text-lg font-semibold mb-4 text-gray-800 dark:text-gray-200 capitalize">
                      🏙️ {city.replace('-', ' ')}
                    </h3>
                    
                    <!-- Categories -->
                    <div class="space-y-3">
                      {Object.entries(categories).map(([category, entries]) => (
                        <div>
                          <h4 class="font-medium text-sm text-gray-700 dark:text-gray-300 mb-2 flex items-center">
                            <span class="mr-2">{getCategoryIcon(category)}</span>
                            {category.replace('-', ' ').split(' ').map(word => 
                              word.charAt(0).toUpperCase() + word.slice(1)
                            ).join(' ')}
                          </h4>
                          <div class="space-y-1">
                            {entries.map((entry) => (
                              <a 
                                href={`/wiki/${country}/${city}/${category}`}
                                class="block text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 text-sm hover:underline transition-colors pl-4"
                              >
                                {entry.data.title}
                              </a>
                            ))}
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Quick Navigation -->
    <section class="mb-12">
      <h2 class="text-2xl font-bold mb-6 text-center">Quick Navigation</h2>
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4">
        <div class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-lg p-4">
          <h3 class="font-semibold mb-2 flex items-center">
            <span class="mr-2">☕</span>
            Cafes & Coworking
          </h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">WiFi speeds, power outlets, and productivity spots</p>
        </div>
        <div class="bg-gradient-to-br from-orange-50 to-red-50 dark:from-orange-900/20 dark:to-red-900/20 rounded-lg p-4">
          <h3 class="font-semibold mb-2 flex items-center">
            <span class="mr-2">🍜</span>
            Food & Dining
          </h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">Local favorites, vegan options, and hidden gems</p>
        </div>
        <div class="bg-gradient-to-br from-blue-50 to-cyan-50 dark:from-blue-900/20 dark:to-cyan-900/20 rounded-lg p-4">
          <h3 class="font-semibold mb-2 flex items-center">
            <span class="mr-2">🏠</span>
            Accommodation
          </h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">Best areas to stay, Airbnb tips, and coliving spaces</p>
        </div>
        <div class="bg-gradient-to-br from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 rounded-lg p-4">
          <h3 class="font-semibold mb-2 flex items-center">
            <span class="mr-2">🎯</span>
            Activities
          </h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">Things to do, networking events, and local experiences</p>
        </div>
      </div>
    </section>

    <!-- Support Section -->
    <section class="bg-gradient-to-r from-purple-50 to-blue-50 dark:from-purple-900/20 dark:to-blue-900/20 rounded-lg p-8 text-center">
      <h2 class="text-2xl font-bold mb-4">💜 Support This Project</h2>
      <p class="text-gray-600 dark:text-gray-400 mb-6 max-w-2xl mx-auto">
        This wiki is completely free and open. If you find it valuable, consider supporting it with SOL for more detailed guides, real-time updates, and personalized recommendations.
      </p>
      
      <div class="max-w-md mx-auto space-y-4">
        <div class="bg-white dark:bg-gray-800 rounded-lg p-4 border">
          <p class="text-sm font-medium mb-2">Solana Wallet Address:</p>
          <code class="text-xs bg-gray-100 dark:bg-gray-700 px-3 py-2 rounded block font-mono text-purple-600 dark:text-purple-400">
            vivgdu332GMEk3FaupQa92gQjYd9LX6TMgjMVsLaCu4
          </code>
        </div>
        
        <div class="text-center">
          <p class="text-sm mb-2">📱 <strong>Telegram for personalized help:</strong></p>
          <a 
            href="https://t.me/vivekpal0x" 
            target="_blank" 
            rel="noopener noreferrer" 
            class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
          >
            <span class="mr-2">💬</span>
            @vivekpal0x
          </a>
        </div>
        
        <p class="text-xs text-gray-500 italic">
          After sending support, DM me for exclusive insider tips, real-time updates, and personalized guidance!
        </p>
      </div>
    </section>
  </main>
</Layout>